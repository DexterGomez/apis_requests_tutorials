This is a simple tutorial to show how to use requests.

To tun it you need to install the environemnt with `pip install requirements.txt`

To run the API you need to deploy it with `uvicorn main:app`

If that does not work, try `python -m uvicorn main:app`